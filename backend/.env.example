PORT=5000
MONGODB_URI=mongodb+srv://USER:PASS@cluster0.xxxxx.mongodb.net/ask-krishna?retryWrites=true&w=majority
JWT_SECRET=your-long-random-secret
FRONTEND_URL=http://localhost:3000

# Production: comma-separated allowed origins (must match your deployed frontend URL(s))
# FRONTEND_URL=https://your-app.vercel.app,http://localhost:3000

# If you get querySrv ECONNREFUSED with mongodb+srv://, use Atlas "Drivers" →
# "mongodb://..." standard connection string (3 hostnames) instead of SRV, or fix DNS (e.g. 8.8.8.8).
